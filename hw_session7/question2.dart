import 'class_Temperature.dart';

void main() {
  //Q2 Create a class Temperature with an attribute celsius. Add a method toFahrenheit()
  //that returns the temperature in Fahrenheit.
  // In main(), create an object and print the converted value.
  Temperature conve = Temperature();
  print(conve.tofahrenhiet(25));
}
